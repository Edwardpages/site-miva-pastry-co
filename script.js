document.addEventListener('DOMContentLoaded', function() { const nav = document.querySelector('.sticky-nav'); const hero = document.querySelector('.hero'); const servicesGrid = document.querySelector('.services-grid'); const testimonials = document.querySelector('.testimonials'); const contactForm = document.querySelector('#contact-form'); // Smooth scroll window.addEventListener('scroll', function() { nav.classList.toggle('scrolled', window.scrollY > 0); }); // Contact form success message contactForm.addEventListener('submit', function(event) { event.preventDefault(); alert('Message sent successfully!'); }); // Fade-in on scroll window.addEventListener('scroll', function() { const elements = document.querySelectorAll('.grid-item, .testimonial'); elements.forEach(function(element) { if (element.getBoundingClientRect().top < window.innerHeight) { element.classList.add('fade-in'); } }); }); })