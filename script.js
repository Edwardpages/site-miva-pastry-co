window.addEventListener('scroll', function() { var scrollPosition = window.scrollY; if (scrollPosition > 100) { document.querySelector('.sticky-nav').style.background = 'var(--primary-color)'; } else { document.querySelector('.sticky-nav').style.background = 'transparent'; } }); document.addEventListener('DOMContentLoaded', function() { var contactForm = document.querySelector('.contact-form'); contactForm.addEventListener('submit', function(event) { event.preventDefault(); var formData = new FormData(contactForm); fetch('/submit', { method: 'POST', body: formData }) .then(function(response) { return response.json(); }) .then(function(data) { console.log(data); contactForm.innerHTML = '<p>Thank you for your message!</p>'; }) .catch(function(error) { console.error(error); }); }); var elements = document.querySelectorAll('.service-card, .testimonial-card'); elements.forEach(function(element) { element.addEventListener('mouseover', function() { element.style.transform = 'scale(1.1)'; }); element.addEventListener('mouseout', function() { element.style.transform = 'scale(1)'; }); }); window.addEventListener('scroll', function() { var scrollPosition = window.scrollY; var elements = document.querySelectorAll('.service-card, .testimonial-card'); elements.forEach(function(element) { if (element.offsetTop - scrollPosition < window.innerHeight) { element.style.opacity = 1; } else { element.style.opacity = 0; } }); });